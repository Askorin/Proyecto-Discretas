#ifndef LINEALIZAR_H
#define LINEALIZAR_H

/* Toma dos valores ordenados (a, b), ambos enteros positivos con a menor a n, y devuelve un número entero positivo. */
int tuple_to_int(int, int, int);

/* Toma un entero positivo c y devuelve un tuple de la forma (a, b) con a, b enteros positivos. y a menor a n. */
int* int_to_tuple(int, int);

#endif
